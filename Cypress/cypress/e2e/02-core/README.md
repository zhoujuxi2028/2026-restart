# ⚙️ 第二阶段：核心功能 (Day 4-8)

## 📖 学习目标

- 掌握高级选择器和查询方法
- 学习元素过滤和遍历
- 精通常用交互命令
- 深入理解断言系统
- 学会使用 Hooks 组织测试

## 📝 测试文件

### Day 4: 选择器和交互
**文件**: `day4-selectors-and-actions.cy.js`

**学习内容**:
- 🔍 高级选择器技巧
- 📋 表单操作（输入、选择、复选框、单选按钮）
- 🖱️ 各种点击操作
- ✅ 更多断言类型

---

### Day 5: 过滤和遍历 (待添加)

**计划学习**:
- `.first()`, `.last()`, `.eq()` - 索引选择
- `.filter()` - 条件过滤
- `.parent()`, `.children()` - DOM 遍历
- `.next()`, `.prev()` - 兄弟元素

---

### Day 6: 常用交互命令 (待添加)

**计划学习**:
- `.type()` - 高级输入技巧
- `.select()` - 下拉选择
- `.check()`, `.uncheck()` - 复选框/单选框
- `.clear()` - 清除输入

---

### Day 7: 断言深入 (待添加)

**计划学习**:
- 存在性断言详解
- 可见性断言
- 状态断言（enabled, disabled, checked）
- 内容断言（text, value, attr, class）
- 链式断言技巧

---

### Day 8: Hooks 和测试组织 (待添加)

**计划学习**:
- `before()` - 所有测试前执行
- `beforeEach()` - 每个测试前执行
- `after()`, `afterEach()`
- `.only()` 和 `.skip()` 的使用
- 测试组织最佳实践

---

## 🎯 本阶段重点

### 1. 选择器策略
```javascript
// 优先级从高到低
cy.get('[data-cy="submit"]')     // 最佳
cy.get('#submit-button')          // 良好
cy.get('.btn-submit')             // 可接受
cy.get('button[type="submit"]')   // 一般
```

### 2. 表单处理
```javascript
// 输入框
cy.get('input').type('text').should('have.value', 'text')

// 下拉菜单
cy.get('select').select('option-value')

// 复选框
cy.get('[type="checkbox"]').check().should('be.checked')

// 单选按钮
cy.get('[type="radio"]').first().check()
```

### 3. 断言组合
```javascript
cy.get('.element')
  .should('exist')
  .and('be.visible')
  .and('contain', 'text')
  .and('have.class', 'active')
```

### 4. Hooks 使用
```javascript
describe('测试套件', () => {
  beforeEach(() => {
    cy.visit('/page')
  })

  it('测试 1', () => {
    // 测试代码
  })

  it('测试 2', () => {
    // 测试代码
  })
})
```

---

## 📊 学习进度检查

完成本阶段后，你应该能够：

- [ ] 熟练使用各种选择器找到页面元素
- [ ] 处理各种类型的表单元素
- [ ] 使用过滤和遍历方法精确定位元素
- [ ] 编写全面的断言验证
- [ ] 使用 Hooks 组织测试代码
- [ ] 理解测试的生命周期

---

## 💡 实战技巧

### 处理动态内容
```javascript
// 等待元素出现
cy.get('.loading').should('not.exist')
cy.get('.content').should('be.visible')
```

### 条件性操作
```javascript
cy.get('body').then(($body) => {
  if ($body.find('.modal').length > 0) {
    cy.get('.modal-close').click()
  }
})
```

### 链式查询
```javascript
cy.get('.container')
  .find('ul')
  .children('li')
  .first()
  .should('be.visible')
```

---

**继续努力！掌握核心功能是成为 Cypress 高手的关键！** 🚀
